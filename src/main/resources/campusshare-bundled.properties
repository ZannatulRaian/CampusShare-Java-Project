# CampusShare — bundled Supabase credentials
# These values are filled in by Maven resource filtering at build time.
# If both are blank, the app runs in offline-only mode.
supabase.url=${supabase.url}
supabase.anon_key=${supabase.anon_key}
